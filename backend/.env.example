# HookedLee Backend Environment Variables
# Copy this file to .env and fill in your actual API keys

# Server Configuration
NODE_ENV=production
HTTP_PORT=3000
HTTPS_PORT=3443

# BigModel API Key (for GLM-4.7, GLM-4.7-FlashX, and image generation)
# Get your key from: https://open.bigmodel.cn/
BIGMODEL_API_KEY=your_bigmodel_api_key_here

# DeepSeek API Key (for DeepSeek-Chat and DeepSeek-Reasoner)
# Get your key from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# WeChat Mini Program Credentials
# Get these from: https://mp.weixin.qq.com/
# Required for user authentication and securing the backend
WECHAT_APP_ID=your_wechat_appid_here
WECHAT_APP_SECRET=your_wechat_appsecret_here

# JWT Secret for token generation
# Generate a strong random string for production
# Use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# SSL Certificate Configuration (HTTPS)
# For local development with self-signed certificates:
#   SSL_KEY_PATH=./ssl/key.pem
#   SSL_CERT_PATH=./ssl/cert.pem
#
# For production with Let's Encrypt:
#   SSL_KEY_PATH=/etc/letsencrypt/live/yourdomain.com/privkey.pem
#   SSL_CERT_PATH=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
#   SSL_CA_PATH=/etc/letsencrypt/live/yourdomain.com/chain.pem (optional)
#
# Generate self-signed cert: npm run generate-cert
# Setup wizard: npm run setup-ssl

# Notes:
# - At least one AI API key must be configured (BIGMODEL_API_KEY or DEEPSEEK_API_KEY)
# - WECHAT_APP_ID and WECHAT_APP_SECRET are required for securing the backend
# - JWT_SECRET must be set to a strong random string in production
# - Never commit .env file to version control
# - Keep your API keys secure!
# - For WeChat Mini Program, HTTPS is REQUIRED in production
