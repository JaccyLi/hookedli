<!-- pages/settings/settings.wxml -->
<view class="container">
  <!-- Header Section -->
  <view class="header">
    <text class="title">{{language === 'en' ? 'Search Settings' : '搜索设置'}}</text>
    <text class="subtitle">{{language === 'en' ? 'Choose your preferred search experience' : '选择您偏好的搜索体验'}}</text>
  </view>

  <!-- Unified Section: Notice + Search Engine Selection -->
  <view class="unified-section">
    <!-- Recommendation Tip -->
    <view class="recommendation-tip">
      <text class="tip-icon">💡</text>
      <view class="tip-content">
        <text class="tip-title">{{language === 'en' ? 'Recommendation' : '推荐'}}</text>
        <text class="tip-text">{{language === 'en' ? 'Standard Search delivers fast, accurate results from our knowledge base.' : '标准搜索从我们的知识库中快速、准确地返回结果。'}}</text>
      </view>
    </view>

    <!-- Section Header -->
    <view class="section-header">
      <text class="section-title">{{language === 'en' ? 'Search Mode' : '搜索模式'}}</text>
      <text class="section-desc">{{language === 'en' ? 'Select how you want to search through articles' : '选择您想要的文章搜索方式'}}</text>
    </view>

    <!-- Search Engine Options -->
    <radio-group class="model-options" bindchange="onModelChange">
      <!-- Standard Search -->
      <label class="model-card {{selectedModel === 'default' ? 'active' : ''}}">
        <radio value="default" checked="{{selectedModel === 'default'}}" class="hidden-radio" />
        <view class="model-card-content">
          <view class="model-icon-wrapper">
            <text class="model-icon">⚡</text>
          </view>
          <view class="model-details">
            <text class="model-name">{{language === 'en' ? 'Standard Search' : '标准搜索'}}</text>
            <text class="model-tags">
              <text class="tag tag-quality tag-recommended">{{language === 'en' ? 'Recommended' : '推荐'}}</text>
              <text class="tag tag-speed tag-recommended">{{language === 'en' ? 'Fast' : '快速'}}</text>
            </text>
            <text class="model-desc">{{language === 'en' ? '✅ Quick results from extensive database • Parallel indexing • Best for daily use' : '✅ 从海量数据库快速检索 • 并行索引 • 适合日常使用'}}</text>
          </view>
          <view class="model-indicator {{selectedModel === 'default' ? 'active' : ''}}">
            <view class="indicator-dot"></view>
          </view>
        </view>
      </label>

      <!-- Premium Search -->
      <label class="model-card {{selectedModel === 'high-quality' ? 'active' : ''}}">
        <radio value="high-quality" checked="{{selectedModel === 'high-quality'}}" class="hidden-radio" />
        <view class="model-card-content">
          <view class="model-icon-wrapper">
            <text class="model-icon">🔬</text>
          </view>
          <view class="model-details">
            <text class="model-name">{{language === 'en' ? 'Premium Search' : '高级搜索'}}</text>
            <text class="model-tags">
              <text class="tag tag-quality tag-best">{{language === 'en' ? 'In-Depth' : '深度'}}</text>
              <text class="tag tag-speed tag-slower">{{language === 'en' ? 'Takes Longer' : '耗时较长'}}</text>
            </text>
            <text class="model-desc">{{language === 'en' ? '✅ Advanced algorithm • Comprehensive analysis • HD cover images' : '✅ 高级算法 • 全面分析 • 高清封面图片'}}</text>
          </view>
          <view class="model-indicator {{selectedModel === 'high-quality' ? 'active' : ''}}">
            <view class="indicator-dot"></view>
          </view>
        </view>
      </label>
    </radio-group>
  </view>

  <!-- Info Section -->
  <view class="info-section">
    <text class="info-title">{{language === 'en' ? 'About Search Modes' : '关于搜索模式'}}</text>
    <view class="info-list">
      <text class="info-item">• {{language === 'en' ? 'Standard: Fast search through curated fly fishing content' : '标准搜索：快速检索精选的飞钓内容'}}</text>
      <text class="info-item">• {{language === 'en' ? 'Premium: Deeper analysis with enhanced visuals' : '高级搜索：更深入的分析和增强的视觉效果'}}</text>
    </view>
  </view>

  <!-- Back Button -->
  <view class="button-container">
    <button class="back-btn" bindtap="goBack">
      <text class="back-btn-icon">←</text>
      <text class="back-btn-text">{{language === 'en' ? 'Back' : '返回'}}</text>
    </button>
  </view>
</view>
